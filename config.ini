[database]
# 数据库类型：mysql 或 sqlite（本地测试用 sqlite，无需安装 MySQL）
type = sqlite

# MySQL 配置（type=mysql 时生效）
host = localhost
port = 3306
user = root
password = 
database = textbook_fee

[admin]
password = admin123
